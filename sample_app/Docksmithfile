# Docksmith sample application
# Uses all 6 instructions: FROM, COPY, RUN, WORKDIR, ENV, CMD

FROM alpine:3.18

WORKDIR /app

ENV APP_VERSION=1.0.0
ENV GREETING=Hello

COPY . .

RUN sh -c "echo 'Build complete. Version: '${APP_VERSION}"

CMD ["sh", "/app/run.sh"]
